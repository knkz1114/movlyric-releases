# movlyric v1.0.0

**movlyric の最初の公開リリースです。**

音源と歌詞を渡すと、歌詞の各単語のタイミング・曲のビート・セクションを自動で推定し、
歌詞アニメーション動画（リリックビデオ）に仕上げます。解析はすべてあなたの PC 上で動き、
音源・歌詞・映像が開発者のサーバーへ送られることはありません。

- 公式サイト: <https://movlyric.konalab.work>
- Discord: <https://discord.gg/YStms87pzy>
- pixivFANBOX: <https://konalab.fanbox.cc/>

---

## できること

### 作る

- **音源と歌詞を入れるだけ**で、単語ごとのタイミング・ビート・セクションを自動推定
- 動画ファイル（mp4/mov/webm/m4v）を選べば、**音声の解析と背景動画の設定を同時に**行う
- 日本語・英語・中国語・韓国語と、その混在に対応
- **ロングトーン検出**: 母音を伸ばして歌っている間、歌詞とカラオケの塗りを歌い終わりまで伸ばす

### 見た目を決める

- **152 種の組み込みスタイル**（ポップ / ロック / バラード / エレクトロニカ / キュート /
  ミニマル / シネマ / レトロ / 和風 / クラブ / シンプル / キネティック）
- **39 種の背景エフェクト** — うち 14 種は曲の周波数解析に連動するイコライザー
- スタイルを **1 から自作できる**。文字の動き・歌詞の置き方・背景エフェクトの 3 要素が
  それぞれ独立していて、キーフレームや図形で自分で組める
- **AI に作らせることもできる**。作りたい動きを言葉で書くとプロンプトが生成され、
  ChatGPT や Claude の返答を貼り戻すと取り込まれる（アプリが AI と直接通信することはありません）
- セクションごと・歌手ごと・フレーズごとにスタイルを変えられる

### 整える

- タイムラインでフレーズと単語のタイミングをドラッグ調整（ビートにスナップ）
- 単語の分割・結合・**ルビ（読み仮名）**
- フレーズ内の**改行位置指定**と 1 文字削除
- 選択した範囲だけ AI で**部分再同期**
- 解析の確信度が低い単語へジャンプする校正ナビ
- 背景クリップ（画像・動画）と前景画像（ロゴ等）の重ね合わせ

### 書き出す

- MP4 (H.264) / WebM (VP9) / MOV (ProRes 互換) / アニメ WebP
- 最大 4K（3840×2160）
- 曲全体・選択フレーズ・ループ範囲から範囲を選んで書き出し
- LRC / TTML / SRT / ASS への歌詞タイミング書き出しと取り込み

### そのほか

- **表示言語は日本語・英語・韓国語・中国語**（起動時と設定画面から切り替え）
- 解析キャッシュの一括・個別削除と、上限を決めた自動削除
- 解析中に **CPU で動いているか GPU で動いているか**を表示

---

## ダウンロード

| ファイル | 対象 | サイズ |
| --- | --- | --- |
| `movlyric-1.0.0-arm64.dmg` | **macOS（Apple Silicon）** | 733MB |
| `movlyric Setup 1.0.0.exe` | **Windows 標準版**（CPU 解析） | 610MB |
| `movlyric Web Setup 1.0.0.exe`<br>`movlyric-1.0.0-x64.nsis.7z` | **Windows CUDA 版**（NVIDIA GPU 解析） | 0.8MB<br>2.26GB |

**Windows CUDA 版は 2 ファイルを同じフォルダに置いてから**
`movlyric Web Setup 1.0.0.exe` を実行してください。片方だけではインストールできません。

NVIDIA GPU が無い場合は標準版を選んでください。

## 動作環境

- **macOS**: Apple Silicon（M1 以降）/ macOS 13 以降。**Intel Mac には対応していません**
- **Windows**: Windows 10 / 11 の 64bit
- 空き容量: 本体 1〜6GB + 音声認識モデル 最大約 3GB + 解析キャッシュ（1 曲 50〜130MB）

**利用には（無料版を含め）Discord ログインと [Discord サーバー](https://discord.gg/YStms87pzy)
への参加が必要です。**

## 無料版とサポーター版

違いは **60fps 書き出し**と**透過（アルファ）書き出し**の 2 点だけです。
形式も 4K も、歌詞編集・解析・スタイル・カスタムの動き / 配置 / 背景エフェクトも、
無料版で制限なく使えます。

サポーター版は [pixivFANBOX](https://konalab.fanbox.cc/) のサポータープラン加入で有効になります。

## インストール時の警告について

**コード署名をしていないため、OS が「開発元を確認できない」警告を出します。**
下記の SHA256 を照合すれば改ざんされていないことを確認できます。

- **macOS**（いずれかの方法で）
  - **方法1**: Finder で `movlyric.app` を右クリック（または Control キー + クリック）→「開く」→
    確認ダイアログで「開く」。この操作は初回のみ必要です
  - **方法2**: ターミナルで `xattr -cr /Applications/movlyric.app` を実行し、
    macOS がダウンロードしたファイルに付与する隔離属性（`com.apple.quarantine`）を削除します
  - macOS 15 以降で方法1 が効かない場合は、一度起動を試みてから
    **システム設定 → プライバシーとセキュリティ**の「このまま開く」を押すか、方法2 を使ってください
- **Windows**: SmartScreen の「詳細情報」→「実行」

## SHA256

```
1b39ed61ef2c3c1080a93a6e204c3a6883f77d64ab5dfe0b592446e099927c65  movlyric-1.0.0-arm64.dmg
5153d7a81a7cae2126303b4002cca5fca027cf6a4cec533f8a1c346a4cbe2070  movlyric Setup 1.0.0.exe
a064752e4c93c305ea1dd7d36413a94187f4d82f42842d8bf3ab1c746cb539cb  movlyric Web Setup 1.0.0.exe
afa3de939db9e9ef17032199bbf2680c3cc0d8d73628f97b65747005a7d7a0e9  movlyric-1.0.0-x64.nsis.7z
```

## 既知の制限

- **Intel Mac には対応していません**（Apple Silicon のみ）
- **自動更新はしません。** 新しいバージョンがあると画面上部にお知らせが出るので、
  このページから入手して入れ直してください
- macOS では歌詞整列に Apple GPU（MLX）を使います。Windows の CUDA 版では
  音源分離と歌詞整列の両方が NVIDIA GPU で動きます
- Windows CUDA 版と標準版は同じ場所にインストールされるため、**併存できません**
  （入れ替えると設定と解析キャッシュはそのまま引き継がれます）

## 権利についてのお願い

**楽曲・歌詞・映像・画像・フォントなど、扱う素材の権利確認と許諾取得は利用者の責任で行ってください。**
制作した動画の公開・配信・営利利用についても同様です。詳しくはアプリ内の
「利用規約・免責事項」を参照してください。

## ライセンス

FFmpeg（LGPL）ほかのオープンソースソフトウェアとフォントを同梱しています。条文と著作権表示は
アプリ内「ライセンス表記」および配布物内の `licenses/` にあります。
同梱 FFmpeg の対応ソースは
[movlyric-ffmpeg-source](https://github.com/knkz1114/movlyric-ffmpeg-source) で提供しています。

Windows CUDA 版は NVIDIA CUDA Toolkit / cuDNN の再頒布ライブラリを含みます
（NVIDIA CUDA Toolkit EULA / cuDNN Software License Agreement）。

## 不具合の報告・質問

[Discord サーバー](https://discord.gg/YStms87pzy) でお願いします。
